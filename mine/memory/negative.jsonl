{"problem_id": "wow2-*", "ansatz": "exhaustive enumeration of all connected graphs, n <= 10, via nauty geng, all ten open WOWII conjectures at once", "why_failed": "no violation exists in that range: 11,988,562 connected graphs scanned across n=3..10, zero candidates emitted", "cost": "~2 minutes of one core"}
{"problem_id": "wow2-141", "ansatz": "exhaustive enumeration of all connected graphs of girth >= 5 (a superset of the girth >= 6 graphs, the only ones where a violation is possible since tree(G) >= maxL+1 forces floor(girth/2) > 2), n <= 15", "why_failed": "no violation: 2,374,364 graphs of girth >= 5 scanned across n=11..15, zero candidates", "cost": "~3 minutes of one core"}
{"problem_id": "wow2-194,wow2-200", "ansatz": "simulated annealing over graphs of fixed order seeded from a random spanning PATH, objective = distance to violation", "why_failed": "seeding from a spanning path guarantees a Hamiltonian path, and both positive controls are non-traceable graphs, so the counterexamples were unreachable by construction. Fixed by seeding from a random attachment TREE. Recorded because it is exactly the class of bug that turns a tooling failure into a false 'the conjecture looks true' conclusion", "cost": "~3 minutes, plus the diagnosis"}
{"problem_id": "wow2-* (ten open)", "ansatz": "generator family 1: dense core (K_q, K_q-e, K_q minus a perfect matching, C_q, P_q) + apex vertices complete to the core + optional vertex joined to two core vertices + pendants; 13,448 graphs, n <= 18", "why_failed": "no violation. This generator DOES pass the positive control (it rediscovers published counterexamples to the refuted conjectures 194 and 200), so the negative is meaningful for this shape", "cost": "~1 minute"}
{"problem_id": "wow2-* (ten open)", "ansatz": "generator family 2: path-length-2 attachments, two cores joined by an edge or path, complete-bipartite and cycle cores; 7,460 graphs, n <= 18", "why_failed": "no violation, but this generator does NOT contain either positive control, so its negative is weak evidence about the instrument rather than about the conjectures", "cost": "~40 seconds"}
{"problem_id": "bound-ramsey-R3333", "ansatz": "any witness at all", "why_failed": "verifier coverage gap, not a search failure: the frozen check_ramsey_lower.py handles only the 2-colour case (one graph, {s,t,graph6}) and has no schema for a 4-colour edge partition, so no witness for R(3,3,3,3) could be verified even in principle. Frozen before this target was selected; not reopened", "cost": "0 (arm abandoned before compute)"}
