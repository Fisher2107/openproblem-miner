=== T3 positive control: build log ===
date: 2026-09-07T04:34:17Z
host: Linux 6.18.44-fc-v24 x86_64, 4 cores

--- toolchain -------------------------------------------------------------
$ export PATH="$HOME/.elan/bin:$PATH"
$ lean --version
Lean (version 4.34.0-rc2, x86_64-unknown-linux-gnu, commit 6a10ac8c22beadecabdbb0919c2b50214762f91d, Release)
Expected exactly one file name
Lean (version 4.34.0-rc2, x86_64-unknown-linux-gnu, commit 6a10ac8c22beadecabdbb0919c2b50214762f91d, Release)
Miscellaneous
  -h, --help             display this message
      --features         display features compiler provides (eg. LLVM support)
  -v, --version          display version information
  -V, --short-version    display short version number
  -g, --githash          display the git commit hash number used to build this binary
      --run <file>       call the 'main' definition in the given file with the remaining arguments
  -o, --o=oname          create olean file
  -i, --i=iname          create ilean file
  -c, --c=fname          name of the C output file
  -b, --bc=fname         name of the LLVM bitcode file
      --stdin            take input from stdin
  -R, --root=dir         set package root directory from which the module name

                         of the input file is calculated

                         (default: current working directory)

  -t, --trust=num        trust level (default: max) 0 means do not trust any macro,

                         and type check all imported modules

  -q, --quiet            do not print verbose messages
  -M, --memory=num       maximum amount of memory that should be used by Lean
                         (in megabytes)
  -T, --timeout=num      maximum number of memory allocations per task
                         this is a deterministic way of interrupting long running tasks
  -j, --threads=num      number of threads used to process lean files
  -s, --tstack=num       thread stack size in Kb
      --server           start lean in server mode
      --worker           start lean in server-worker mode
      --plugin=file[=fn] load and initialize Lean shared library for registering linters etc.
      --load-dynlib=file load shared library to make its symbols available to the interpreter
      --setup=file       JSON file with module setup data (supersedes the file's header)
      --json             report Lean output (e.g., messages) as JSON (one per line)
  -E, --error=kind       report Lean messages of kind as errors
      --deps             just print dependencies of a Lean input
      --src-deps         just print dependency sources of a Lean input
      --print-prefix     print the installation prefix for Lean and exit
      --print-libdir     print the installation directory for Lean's built-in libraries and exit
      --profile          display elaboration/type checking time for each definition/theorem
      --stats            display environment statistics
      --incr-save=file   EXPERIMENTAL: save a full incremental snapshot of post-elaboration state at end of run
      --incr-load=file   EXPERIMENTAL: reuse a snapshot saved by `--incr-(header-)save` at start of run
      --incr-header-save=file
                         EXPERIMENTAL: like `--incr-save`, but save only the header (state after importing)
      -D name=value      set a configuration option (see set_option command)
(no lakefile, no imports: the file is checked by plain , standing on the
 Lean 4 prelude only.  Full mathlib is NOT available in this container.)

--- graph decoding, independent of Lean -----------------------------------
$ echo 'J^~~u?_C?O?' | nauty-listg -a

Graph 1, order 11.
00111111000
00111111000
11011110100
11101110010
11110110001
11111000000
11111000000
11000000000
00100000000
00010000000
00001000000

--- the Lean file's transcription of that matrix, re-checked externally ----
$ python3 (compare listgRow / rowMask literals in Conj200Control.lean with nauty output)
listgRow literals == nauty adjacency matrix : True
rowMask literals   == bitmasks of that matrix: True
/bin/bash: line 85: adj_matches_listg: command not found
(inside Lean, theorem  re-checks rowMask against listgRow by decide)

--- verifier freeze (nothing here writes to mine/verify) -------------------
$ bash /home/user/openproblem-miner/scripts/check-freeze.sh
VERIFIER INTACT (40 files)

--- no sorry, no native_decide, no added axioms ----------------------------
$ grep -n "sorry\|native_decide\|admit\|axiom " Conj200Control.lean ; echo "exit=$?"
exit=1 (1 = no matches)

--- BUILD -----------------------------------------------------------------
$ cd /home/user/openproblem-miner/mine/attacks/t3-positive-control
$ time lean Conj200Control.lean
'conj200_counterexample' depends on axioms: [propext, Quot.sound]
'conj200_counterexample_explicit' depends on axioms: [propext, Quot.sound]
'G_connected' depends on axioms: [propext]
'G_treeNumber' depends on axioms: [propext, Quot.sound]
'l_all' depends on axioms: [propext, Quot.sound]
'no_ham_path' depends on axioms: [propext, Quot.sound]
'adj_matches_listg' depends on axioms: [propext]

real	0m52.367s
user	0m25.234s
sys	0m7.272s

The seven lines above are the output of the `#print axioms` commands at the end of the
file (there is no other output: the build is warning-free and error-free).

  no `sorryAx`       => no `sorry` anywhere in the dependency graph of these theorems;
  no `ofReduceBool`  => `native_decide` was NOT used; every `decide` was replayed by the
                        Lean kernel itself;
  no `Classical.choice` either.  `propext` and `Quot.sound` are two of the three standard
  Lean axioms, pulled in by `simp`/`omega`; both are part of Lean's trusted core.
